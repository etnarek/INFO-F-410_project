node PID(VALUE, TARGET, KP, KI, KD : real) returns (CORRECTION : real);
var ERROR, INTEG, DERIV : real;
let
  ERROR = 0.0 -> TARGET - VALUE;
  INTEG = 0.0 -> pre(INTEG) + ERROR;
  DERIV = ERROR - pre(ERROR);

  CORRECTION = KP * ERROR + KI * INTEG + KD * DERIV;
tel
